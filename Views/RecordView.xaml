<Window x:Class="YourNamespace.RecordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="設備紀錄" Height="400" Width="600">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding DeviceRecords}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="裝置名稱" Binding="{Binding DeviceName}" Width="*"/>
                <DataGridTextColumn Header="使用者" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="內容" Binding="{Binding Content}" Width="2*"/>
                <DataGridTextColumn Header="時間" Binding="{Binding Timestamp}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
